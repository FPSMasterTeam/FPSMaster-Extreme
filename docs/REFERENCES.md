# References

Local references are cloned under `references/` and ignored by Git.

Currently cloned/attempted:

- `references/MCP-919` from `https://github.com/Marcelektro/MCP-919.git` using sparse clone. This provides MCP scripts; generated/decompiled Minecraft sources are not committed.
- `references/minecraft-data` from `https://github.com/PrismarineJS/minecraft-data.git` for protocol/data cross-checking.
- `references/node-minecraft-protocol` from `https://github.com/PrismarineJS/node-minecraft-protocol.git` for protocol behavior reference.
- `references/Leafish` from `https://github.com/Lea-fish/Leafish.git` as a Rust Minecraft client reference.
- `references/minecrust` from `https://github.com/glasss13/minecrust.git` as a small 1.8.9 protocol client reference.

Do not vendor or commit Mojang code/assets into this repository.
