[package]
name = "my_native_mod"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Bundled with this SDK (no crates.io needed). To move the mod elsewhere, copy
# the `recraft_ext_api/` folder along with it, or switch to a git dependency:
#   recraft_ext_api = { git = "https://github.com/gaoyu06/MiniCraft" }
recraft_ext_api = { path = "../recraft_ext_api" }
# abi_stable's macros expand to `::abi_stable` paths, so depend on it directly at
# the same version recraft_ext_api uses.
abi_stable = "0.11"
